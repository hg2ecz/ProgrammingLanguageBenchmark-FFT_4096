pkgname=fft-benchmark
pkgver=1.6.1
pkgrel=1
epoch=
pkgdesc="The FFT benchmark from Zsolt Krüpl"
arch=('aarch64')
url="https://github.com/hg2ecz/ProgrammingLanguageBenchmark-FFT_4096"
license=('')
groups=()
depends=(icu)
makedepends=()
checkdepends=()
optdepends=()
provides=()
conflicts=()
replaces=()
backup=()
options=()
changelog=
source=()
noextract=()
md5sums=()
validpgpkeys=()
install=fft-benchmark.install

package() {
	cd ".."
	make DESTDIR="$pkgdir/opt/fft-benchmark" ARCH=arm64 VERSION=$pkgver -f Makefile
	mkdir -p /usr/bin
}

post_install() {
	ln -s /opt/fft-benchmark/fft-benchmark /usr/bin/
}

pre_remove() {
	rm -rf /usr/bin/fft-benchmark
}
