{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make",
            "type": "shell",
            "command": "rm -rf CMakeFiles; cmake -DCMAKE_BUILD_TYPE=Debug -G 'Unix Makefiles'; make",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}